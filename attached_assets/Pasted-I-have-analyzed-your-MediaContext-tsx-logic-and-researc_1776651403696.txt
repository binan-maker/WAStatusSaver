I have analyzed your MediaContext.tsx logic and researched the common "Empty Status" issue on Android 11, 12, and 13+.
Despite having the "permission granted," the reason your home page remains empty usually comes down to how Android handles hidden folders and the specific path transition introduced in Scoped Storage.
1. The Root Cause: The "Hidden" Folder Ghosting
On Android, folders starting with a dot (like .Statuses) are considered hidden. When you use the Storage Access Framework (SAF) in Expo:
The Indexing Lag: Even if the constant points to the right path, Android's MediaProvider often hasn't "indexed" the contents of a hidden folder for a third-party app immediately after permission is granted.
Permission vs. Access: Granting permission to Android/media/com.whatsapp/... is not the same as granting permission to the .Statuses folder itself. If the user selects the "WhatsApp" folder but the app doesn't "walk" deep enough into the sub-directory to find the hidden folder, it will return an empty list.
2. Why your code shows "No images" even after refresh
Based on your implementation in MediaContext.tsx, there are three likely technical failures:
A. The "Android/media" URI Mismatch
Android 11+ changed the path from /WhatsApp/Media to /Android/media/com.whatsapp/WhatsApp/Media.
The Issue: If your SAF_INITIAL_URIS constant uses the old path, the user might be granting permission to a folder that no longer receives status updates.
The Fix Strategy: You must ensure the app checks for the existence of the Android/media path first. If it exists (Android 11+), it must force the user to that specific sub-directory.
B. The "BFS" (Breadth-First Search) Cache Problem
In your current code, you have resolvedUriCache.current.delete(result.directoryUri).
The Issue: If the first "walk" through the folder tree fails to find .Statuses (because Android was still "mounting" it), and that empty result gets cached or the loadStatuses function doesn't trigger a hard re-scan, the app will stay empty forever.
The Logic Gap: Your code waits 700ms, but sometimes Android takes up to 3 seconds to "materialize" the files in a hidden folder after a fresh permission grant.
C. The File Extension Filter
If readFromSAF is strictly looking for .jpg or .mp4, it might miss them if the SAF provider returns a URI without an extension (which happens often in the .Statuses folder).
The Issue: SAF often provides a lastModified and size, but the name might be just a hash. If your filtration logic is too strict on the filename, it skips everything.
3. How to fix this (Methodology)
To solve this without breaking the user experience, changes are needed in the "Walking" logic of MediaContext.tsx:
Deep-Link Validation: Instead of just trusting the directoryUri returned by the user, the code must verify if the URI ends in WhatsApp%2FMedia%2F.Statuses. If it doesn't, the code must perform a Recursive Search (walking through children) to find a folder named .Statuses.
MIME-Type over Extension: Instead of checking if a file ends in .jpg, the code should check the mimeType property returned by readDirectoryAsync. Android is much more reliable at reporting "image/jpeg" than it is at keeping the ".jpg" extension in hidden folders.
The "Work Profile" Trap: If a user has "Dual WhatsApp," they have two different Android/media folders. Your code currently doesn't distinguish between com.whatsapp and com.whatsapp.w4b (Business). If the user grants permission to one but is looking for statuses from the other, they see nothing. You need to provide a way to grant permission for both.
Summary for your next step:
Verify your SAF_INITIAL_URIS and ensure readFromSAF is performing a Deep Search for the .Statuses folder rather than just looking at the top-level files of the granted directory. If the user grants access to the "WhatsApp" folder, your code is currently standing at the front door while the statues are locked in the basement (.Statuses). You need to tell the code to go into the basement.
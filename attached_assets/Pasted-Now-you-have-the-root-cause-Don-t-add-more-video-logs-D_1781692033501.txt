Now you have the root cause.

Don't add more video logs. Don't touch ExoPlayer. Don't touch buffering.

The bug is:

```text
active
background
active
background
active
background
```

while the viewer is open.

A normal app should be:

```text
active
(active for entire playback)
active
active
```

or at most:

```text
active
background   <-- user leaves app
active       <-- user returns
```

---

### Step 1: Confirm 100%

Temporarily remove AppState from pause logic:

```ts
const paused =
  !isActive ||
  manuallyPaused;
```

instead of:

```ts
const paused =
  !isActive ||
  appState !== "active" ||
  manuallyPaused;
```

Run.

### Result A

If video becomes smooth:

```text
plays normally
no freeze
no pause/play loop
```

then AppState oscillation is the cause.

Stop investigating video code.

---

### Step 2: Find who triggers AppState

Search project-wide:

```bash
grep -R "AppState" .
```

or VSCode search:

```text
AppState
```

Show me every file that contains:

```ts
AppState
```

especially:

```ts
AppState.addEventListener
AppState.currentState
```

---

### Step 3: Search for fullscreen/system UI code

Search entire project:

```text
StatusBar
```

```text
NavigationBar
```

```text
immersive
```

```text
fullscreen
```

```text
setSystemUiVisibility
```

```text
react-native-system-navigation-bar
```

```text
react-native-navigation-bar-color
```

```text
expo-navigation-bar
```

```text
WindowInsets
```

```text
setHidden
```

---

### What I suspect

You previously mentioned "dark bars fix".

The timing matches exactly.

Something is repeatedly trying to hide/show:

```ts
StatusBar
NavigationBar
```

Android loses window focus for a moment:

```text
FOCUS
BLUR
FOCUS
BLUR
```

React Native interprets that as:

```text
active
background
active
background
```

and your player pauses/resumes forever.

---

### Most likely files

Open these first:

```text
app/viewer.tsx
```

```text
App.tsx
```

```text
RootLayout.tsx
```

```text
_layout.tsx
```

```text
navigation setup
```

and any file where you previously implemented:

```text
dark bars
immersive mode
fullscreen mode
status bar hiding
navigation bar hiding
```

---

### The fastest path now

1. Remove AppState from video pause logic.
2. Verify video becomes smooth.
3. Send me:

   * all files containing `AppState`
   * all files containing `StatusBar`
   * all files containing `NavigationBar` / `immersive`

Once I see those, we can identify the exact line causing the active/background loop.
